#to checkout pr in one's own repo

first
1. git checkout main
2. git pull origin main

3. git checkout -b new_branch   \\to create a new branch
4. branch created, do whatever changes you want.

5. git status
6. git add .
7. git commit -m "update"

8. git push origin main new_branch
This creates a new remote branch on GitHub.

Go to your repo in the browser.
You’ll see a yellow box:
add-visualization had recent pushes → Compare & pull request

configure the PR

Merge pull request
Confirm merge

git checkout main
git pull origin main since our local machine is outdated

